# frozen_string_literal: true

source "https://rubygems.org"

# Gem dependencies (runtime and development) live in kino.gemspec.
gemspec

gem "irb"

# Benchmark baselines (bench/run.sh).
gem "puma", "~> 8.0"
gem "falcon", "~> 0.52"
